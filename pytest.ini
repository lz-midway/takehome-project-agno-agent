[pytest]
# Always run pytest from the repo root: python -m pytest tests/ -v
# pythonpath adds the repo root to sys.path so "from app.x import y" works
# regardless of which directory pytest is invoked from.
pythonpath = .
testpaths = tests
